//sum of all the numbers till the given number.
#include <stdio.h>
int main()
{
    int i,j,sum=0;
    printf("Enter the number:\t");
    scanf("%d",&i);
    
    for(j=0;j<=i;j++)
    {
        sum=sum+j;
    }
    printf("The sum of numbers is: %d",sum);
    return 0;
}
