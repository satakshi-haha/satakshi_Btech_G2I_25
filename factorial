//Factorial of the given number.
#include <stdio.h>
int main()
{
    int i,j;
    int long fact=1;
    printf("Enter the number:\t");
    scanf("%d",&i);
    
    for(j=1;j<=i;j++)
    {
        fact=fact*j;
    }
    printf("The factorial of %d is : %ld",i,fact);
    return 0;
}
