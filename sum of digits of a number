//sum of digits of a number.
#include <stdio.h>
int main()
{
    int n,sum=0,dig;
    printf("Enter the number:\t");
    scanf("%d",&n);
    while(n>0)
    {
        dig=n%10;
        sum=sum+dig;
        n=n/10;
    }
  
        printf("The sum of the number is: %d",sum);
       
    return 0;
}
